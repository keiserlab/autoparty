name: autoparty-env
channels:
  - defaults
  - conda-forge   
dependencies:
  - rdkit
  - openbabel=3.1.0
  - python=3.7
  - networkx
  - pip
  - pip:
      - Flask
      - Flask-Migrate
      - Flask-Login
      - Flask-WTF
      - celery
      - torch
      - python-decouple
      - scipy
      - xopen
      - pdbecif
      - colorlog
      - mmh3==2.5.1
      - scikit-learn
      - tqdm
      - redis
      - importlib-metadata==4.13.0
      - gunicorn
      - biopython==1.72
prefix: autoparty-env
